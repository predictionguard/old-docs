# REST API Reference

All of the Prediction Guard functionality is available via a REST API. This API is organized as follows:

- `/user` - Manage your user
- `/proxy` - Create and manage proxy endpoints
- `/predict` - Use endpoints to make predictions

The root URL for all API calls is `https://api.predictionguard.com`.